/**
 * Interaction with the AR scene using touch events.
 * 
 * Detects touch inputs on the devices and translates them into AR actions.
 */
export class TouchInteraction {
    
    public constructor() {

    }
}